[build]
base = "docs"                       # <- run all steps inside docs/
command = "npm install && npm run docs:build"
publish = ".vitepress/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
